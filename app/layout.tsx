import type { Metadata } from 'next'
import { Space_Grotesk, Inter, JetBrains_Mono } from 'next/font/google'
import './globals.css'

const spaceGrotesk = Space_Grotesk({
  subsets: ['latin'],
  variable: '--font-space-grotesk',
  display: 'swap',
})

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-jetbrains',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Creator Stack — Sovereign Creator OS',
  description:
    'Creator OS, Prompt Architect, and Manuscript Studio — three AI-powered tools unified into one sovereign creator platform. Built by Sovereign Shield Technologies.',
  openGraph: {
    title: 'Creator Stack — Sovereign Creator OS',
    description:
      'Three AI-powered creator tools in one sovereign platform. Built for founders, authors, and entrepreneurs who ship without a team.',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html
      lang="en"
      className={`${spaceGrotesk.variable} ${inter.variable} ${jetbrainsMono.variable}`}
    >
      <body>{children}</body>
    </html>
  )
}
